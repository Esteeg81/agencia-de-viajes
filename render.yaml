services:
  - type: web
    name: agencia-de-viajes
    env: node
    plan: free
    buildCommand: npm install && npm run build:all
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SERPAPI_KEY
        sync: false
